# Roadmap

Future changes must preserve the calibration/embargo/test invariant and offline research boundary.

- Add deterministic moving-block bootstrap intervals for score differences.
- Add an opt-in rolling-origin evaluator without overlapping with strategy backtesting.
- Add a versioned JSON Schema for configuration, rows, and reports.
- Evaluate constrained stacking objectives that combine log loss and ordered RPS without retrospective metric selection.
